# CreatorOS - AI 视频爆款复制工具

## 产品定位

**一句话**: 给一条爆款短视频 URL，自动分析并"复刻"出一条风格类似的新视频。

面向短视频创作者/运营团队，解决"看到爆款想模仿但不知道怎么做"的痛点。

---

## 核心流程

```
爆款视频 URL
    ↓
[1] 下载视频（去水印）
    ↓
[2] 提取字幕（Qwen VL 语音转写）
    ↓
[3] 逆向 Prompt（AI 分析画面风格、运镜、分镜）
    ↓
[4] 仿写剧本（学习结构节奏，原创文案）
    ↓
[5] 生成视频 Prompt（翻译成视频模型可用格式）
    ↓
[6] 生成视频（小云雀/即梦 API）
    ↓
[7] 发布包（视频 + 文案 + 话题标签 + 发布指南）
```

---

## 技术架构

### 后端 Pipeline

| 阶段 | 模块 | 功能 | AI 模型 |
|------|------|------|---------|
| 下载 | `video_downloader.py` | 抖音/小红书去水印下载 | - |
| 字幕 | `subtitles.py` | 语音转文字，带时间轴 | Qwen VL |
| 逆向 | `reverse_prompt.py` | 分析视频风格、分镜、运镜 | Qwen VL |
| 仿写 | `script_writer.py` | 基于账号定位仿写新剧本 | Qwen VL |
| Prompt | `video_prompt_builder.py` | 剧本→视频生成 Prompt | Qwen VL |
| 生成 | `video_generator.py` | 调用视频生成 API | 小云雀 Seedance 2.0 |
| 发布 | `publisher.py` | 生成发布包 | - |

### 关键设计

**可干预重跑机制**

每个阶段产物都是独立 JSON 文件：
```
runs/<timestamp>_<title>/
├── 01_subtitles.json      # 字幕
├── 02_reverse_prompt.json # 逆向分析
├── 03_script.json         # 仿写剧本
── 04_video_prompts.json  # 视频 Prompt
├── 05_generation_result.json # 生成结果
├── videos/final_video.mp4 # 最终视频
└── publish_package/       # 发布包
```

用户可以在任意阶段暂停，手动修改中间产物（如调整剧本、优化 Prompt），然后从该阶段继续执行，无需从头开始。

**多模型支持**

- **分析层**: Qwen VL（通义千问视觉语言模型）、Gemini
- **生成层**: 小云雀 Seedance 2.0（带配音成片）、即梦（纯画面）
- **下载层**: you-get、videofetch 多解析器回退

---

## 产品亮点

### 1. 逆向工程爆款

不是简单复制，而是 AI 理解爆款的"为什么火"：
- 画面风格（色调、质感、光线）
- 运镜手法（推拉摇移、固定、跟随）
- 节奏结构（钩子、转折、CTA）
- 情绪氛围

### 2. 原创仿写

学习爆款的**结构和节奏**，但文案完全原创：
- 结合账号定位/调性
- 贴合目标受众
- 避免照搬原文案

### 3. 一键出成片

小云雀 Seedance 2.0 直接生成带配音、音效、口型对齐的完整视频，不是逐段分镜拼接。

### 4. 平台化发布

自动生成适配抖音/小红书的发布包：
- 视频文件（9:16 竖屏）
- 发布文案（含 emoji）
- 话题标签
- 发布指南（最佳发布时间、封面建议）

---

## 使用场景

### 场景 1: 快速模仿竞品爆款
```
1. 找到竞品爆款视频链接
2. 粘贴到 CreatorOS
3. 等待 5-10 分钟
4. 获得风格类似的全新视频 + 发布文案
```

### 场景 2: 批量生产同类型内容
```
1. 分析一条爆款，得到逆向 Prompt
2. 修改剧本，换主题/产品
3. 重新生成视频
4. 批量产出系列内容
```

### 场景 3: 人工优化中间产物
```
1. 执行到"仿写剧本"阶段
2. 手动调整剧本内容
3. 继续执行后续阶段
4. 获得更精准的结果
```

---

## 技术栈

- **后端**: Python + FastAPI
- **前端**: Vue 3 + Tailwind CSS
- **AI 模型**: Qwen VL、Gemini、小云雀 Seedance 2.0
- **视频处理**: ffmpeg（压缩）、you-get（下载）
- **云服务**: 火山引擎（即梦/小云雀）、阿里云 DashScope（Qwen）

---

## 依赖服务

| 服务 | 用途 | 配置项 |
|------|------|--------|
| 阿里云 DashScope | Qwen VL 视频理解 | `QWEN_API_KEY` |
| 火山引擎 | 即梦/小云雀视频生成 | `JIMENG_ACCESS_KEY_ID`, `JIMENG_SECRET_ACCESS_KEY` |
| Google Gemini | 可选的视频分析模型 | `GEMINI_API_KEY` |

---

## 部署方式

```bash
# 1. 安装依赖
pip install -r requirements.txt

# 2. 配置环境变量
cp .env.example .env
# 编辑 .env 填入 API Keys

# 3. 启动服务
python server.py

# 4. 访问前端
http://localhost:8001
```

---

## 后续规划

### 短期
- [ ] 支持更多平台（快手、B 站、TikTok）
- [ ] 批量处理模式（一次提交多个 URL）
- [ ] 视频质量评分（生成后自动评估）

### 中期
- [ ] 模板市场（用户分享成功的逆向 Prompt）
- [ ] A/B 测试（同一剧本生成多个版本）
- [ ] 数据回流（发布后追踪播放量，优化模型）

### 长期
- [ ] 自动发布（对接平台 API 直接发布）
- [ ] 团队协作（多账号、多角色权限）
- [ ] 商业化（SaaS 订阅制）

---

## 竞品对比

| 特性 | CreatorOS | 传统剪辑工具 | 其他 AI 视频工具 |
|------|-----------|-------------|-----------------|
| 爆款逆向 | ✅ AI 自动分析 | ❌ 手动拆解 | ⚠️ 部分支持 |
| 原创仿写 | ✅ 结合账号定位 | ❌ 无 | ⚠️ 简单改写 |
| 一键成片 | ✅ 带配音音效 | ❌ 手动剪辑 | ️ 仅画面 |
| 可干预 | ✅ 每步可修改 | ✅ 完全控制 | ❌ 黑盒 |
| 发布包 | ✅ 文案 + 标签 | ❌ 无 | ❌ 无 |

---

## 核心指标

- **处理时长**: 5-10 分钟/条（主要耗时在视频生成）
- **成功率**: >85%（依赖 API 稳定性）
- **成本**: 约 0.5-2 元/条（API 调用费用）

---

## 团队

- 产品/开发: Vincent
- 技术栈: Python/Vue/Cloud APIs

---

*最后更新: 2026-06-10*
